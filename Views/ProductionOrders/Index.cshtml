@model IEnumerable<CameraCounter.Models.ProductionOrder>

@{
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.CamerasMade)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.CamerasFailed)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.DateDone)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Line)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.CamerasMade)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.CamerasFailed)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.DateDone)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Line.ID)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.ID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.ID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.ID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
